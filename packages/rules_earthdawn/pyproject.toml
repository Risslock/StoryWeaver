[project]
name = "storyweaver-rules-earthdawn"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["storyweaver-core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
storyweaver-core = { workspace = true }