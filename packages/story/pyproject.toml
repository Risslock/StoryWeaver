[project]
name = "storyweaver-story"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "storyweaver-core",
    "storyweaver-storage",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["story"]

[tool.uv.sources]
storyweaver-core = { workspace = true }
storyweaver-storage = { workspace = true }